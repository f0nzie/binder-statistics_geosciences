# NEWS

Because of many conflicts installing special package with `pip`, we switched to `conda`.
Packages requiring special dependencies are:
* importlib -> importlib_resources
* mpl_toolkits.basemap -> basemap